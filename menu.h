#include <stdio.h>
#include <stdlib.h>
#include "colori.h"

int menu_iniziale(int);
int primo_menu_utente(int);
int primo_menu_admin(int);
int menu_utente(int , char []);
int menu_admin(int , char []);
